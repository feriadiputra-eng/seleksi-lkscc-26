# This Lambda function will automatically create the table and insert fixed data every time it runs.
# Add values ​​in code to insert data
nama = ""
kelas = ""
sekolah = ""
gender = ""

# Create Layer for Lambda Function